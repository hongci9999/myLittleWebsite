@if, for, while, 표준 라이브러리

# 선텍제어문

## if else
참인 경우 블록 구조의 코드 실행

삼항 연산자
//예시

## switch case default

switch
단일 값돠 비교
데이터가 일치하면 해당 영영과 아래 모든 코드 함꼐 실행
//예시

default
가장 아래 위치 
일치하는 데이터가 있는 경우에도 수행

break
일치하는 경우 데이터 블록 나감

# 반복문
for( 초기시기;  종료분기; 증감식){

}

continue
아래 코드를 무시하고 다음 단계 반복 진행

## 조건 제한 반복문
while
조건을 만족하는 동안 반복

do-while
코드 실행 후 조건 확인 
최소 1회 반복
//예시

무한 
loop
c - while(1)
java - while(true)
pyth - while True:

# 함수
호출한 프로그램은 잠시 멈춤

표준 라리브러리
미리 개발된 함수들을 기능별로 묶은 라이브러리
.#include를 이용해 코드에 넣음
stdio.h 데이터 입출력
math.h  - sqrt(), pow(), abs()
string.h - strlen(),strcopy(), strcmp()
stdib.h - 기본 데이터 관련

함수의 정의와 호출
main 함수 밖에서 정의

반환갑이 없으면 void


인수 -외부에서 함수로 넘겨주는 값
매개변수 - 이 인수를 할당받은 변수

값에 의한 전달

값에 의한 전달 call by value
//예시