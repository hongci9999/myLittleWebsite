@ 타입변환, 시스템 카탈로그, 반정규화

# 물리 데이터 모델 변환
## 개체 테이블로 변환
기본키와 외래키를 통해 다른 테이블과의 관계 설정

식별자 - 후보키
주식별자 - 기본키
보조 식별자 - 대체키
외례 식별자 - 외레키

슈퍼타입 기준 변환
서브 타입을 슈퍼 타입에 통합

서브 타입 기준 변환
각각의 서브 타입에 추가하여 서브 타입 별 하나의 테이블로 변환
서브 타입에 많은 양의 속성이나 관계를 가진 경우

개별 타입 기준 변환
슈퍼 타입과 거브 타입을 각가의 테이블로 변환

전체 데이터에 대한 처리가 자주 발생하는 경우
통합하는 테이블의 컬럼 수가 지나치네 많은 경우
트랜잭션이 주로 슈퍼타입에서 발생하는 경우


## 관계 변환
1:m 변환
가장 보편적

1:1 변환

1:m 순환 관계 변환
기본키를 참조하는 외래키 칼럼을 추가
데이터의 계층 구조 표현

배타적 관계 변환
외래키의 분리 혹은 결합을 통해 변환
//예시 추가
외래키 분리 - 관계별로 칼럼을 생선
결합 - 하나의 컬럼으로 통합, 별로 컬럼 필요

## 컬럼 변환
관리 목적 컬럼 추가
업무 수행 속도 향상을 위해


## 데이터 표준 ㅈ적용
데이터 명명 규칙
영문명으로 전환
영문 약어 사용

# 데이터베리스 반정규화
운영의 단순화를 위해 중보그 통하브 분할 등 수행
성능을 위해
다햐ㅕㅇ 범위나 특정 범위 데이터만 자주 처리하는 경우
집계 정보가 자주 요구되는 경우

## 유형
중복 데이터 추가
자주 처리되거나 많은 양의 데이터 자주 처리

테이블 조합
둘 이상의 테이블에서 진행되는 경우

테이블 분할
칼럼의 사용빈도가 높은 경우에

테이블 제거
더 이상 사용하지 않는 경우
관리 소홀이 주된 원인

컬럼의 중복화
자주 사용되는 컬럼이 서로 다른 테이블에 분산되어있는 경우
다른 방법 우선 검토