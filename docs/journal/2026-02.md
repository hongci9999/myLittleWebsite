# 2026년 2월 개발 로그

## 2026-02-17

### 한 일

- 프로젝트 Cursor 룰 설계 및 작성 (foundation, git, stack-structure, docs-record)
- 기술 스택 결정: React+TS, Node+Express, Supabase
- docs 폴더 구조 및 기록 체계 수립
- 의사결정 기록 2건 작성 (0001 기술 스택, 0002 프로젝트 기초 구조)

### 다음 할 일

- Supabase 프로젝트 생성 및 연동

---

## 2026-02-19

### 한 일

- client 폴더: Vite + React + TypeScript 세팅 (Vite로 가벼운 빌드)
- server 폴더: Express + TypeScript, tsx watch로 개발 모드
- 루트 npm workspaces 구성, `npm run dev`로 client·server 동시 실행
- /api/\* 프록시 설정 (client 5173 → server 3000)
- docs CHANGELOG 반영

### 다음 할 일

- Superpowers 플러그인 설치 및 실제 기능 개발에 적용해보기

---

## 2026-02-20

### 한 일

- Superpowers (obra/superpowers) 플러그인 도입 결정
- 에이전트 워크플로우: brainstorming → plans → TDD → subagent-driven-development
- 의사결정 기록 0003 작성 (Superpowers 적용)

### 배경·목적

- 코드 작성 전 설계·계획 선행으로 foundation "이유 기반" 원칙 강화
- 체계적 접근·TDD를 자동 트리거하는 플러그인 활용

### 다음 할 일

- 실제 기능(예: Supabase 연동) 개발 시 워크플로우 적용해보기

### 추가 (이후)

- Superpowers 프로젝트 전용 설치 완료
  - `.cursor/skills/superpowers` 클론 (SOY4RIAS Gist 기준)
  - `.cursor/rules/superpowers-bootstrap.mdc` 생성 (경로: `.cursor/skills/...` 프로젝트 상대)
  - learnings 0002: PowerShell CLI, Bootstrap 출처(Gist), 프로젝트 전용 절차 추가

---

## 2026-02-23

### 한 일

- **Feature-Sliced Design** 학습 정리: `docs/learnings/0004-feature-sliced-design.md` 생성
- **학습 문서 태그 시스템** 도입: docs-record 룰에 태그 규정 추가, 기존 learnings 4개에 태그 부여
- **AI 관련 태그** 추가: `에이전트`, `mcp`, `스킬` (추후 DB·필터링용)
- **FSD 적용 전략** 룰·문서화: `stack-structure.mdc`에 점진적 적용 가이드, `decisions/0004` 작성
- **디자인 시스템** 결정: shadcn/ui 채택 (`decisions/0005`)
- **디자인 시스템 비교 페이지** 삭제·임시 메인: 결정 완료 후 삭제, 단순 메인 페이지만 유지
- **shadcn/ui 적용**: Tailwind v4, Button 컴포넌트, Neutral 테마(다크), `components.json`, `lib/utils.ts`
- **react-refresh** 버튼 export 오류: `buttonVariants`와 컴포넌트 혼합 export → eslint-disable 처리
- **ESLint + Prettier** 설정: `.prettierrc`, `.prettierignore`, `eslint-config-prettier`, 루트 `format`/`format:check` 스크립트
- **react-refresh 규칙** 전역 비활성화: shadcn 컴포넌트 패턴(컴포넌트+variants export) 허용, button.tsx eslint-disable 제거

### 배경·목적

- FSD: foundation의 extensibility와 맞는 아키텍처, 점진적 도입으로 오버엔지니어링 방지
- 학습 태그: 나중에 ai·프론트·알고리즘 등으로 분류·DB화할 수 있도록
- shadcn/ui: copy-paste, Tailwind, 커스터마이징 자유도

### 막힌 점·해결

- npm install: 샌드박스 권한 문제로 실패 → 사용자 로컬에서 `npm install` 필요
- shadcn init: CLI 미실행, 직접 `components.json`·설정 파일 작성

### 다음 할 일

- `npx shadcn@latest init` 실행 여부 검토 (선택)
- 추가 컴포넌트 필요 시 `npx shadcn@latest add <이름>` 실행
- `npm run format` 커밋 전 실행 권장

---

## 2026-02-26

### 한 일

- **디자인 플레이그라운드** 구현 (`/design-playground`)
  - 폰트(본문 7종, 코드 8종), 색상 테마(복수 선택, 20+ 테마), 모서리·그림자·트랜지션
  - 컴포넌트 미리보기: Button, Card, 선택지(단일/다중/토글 유형별), 메뉴, 코드블록, 입력칸, 타이포그래피(굵기·줄간격·제목/본문 크기), 탭, 배지, 토글, 페이지네이션, 프로그레스, 토스트, 드롭다운
  - 선택 결과 마크다운 복사 (AI 전달용)
- **모서리(radius)** 미리보기 반영: `--radius` CSS 변수를 미리보기 컴포넌트에 적용
- **선택지** 유형별 분리: 단일선택/다중선택/토글을 독립 섹션으로, 각각 스타일 선택
- **타이포그래피** 제목·본문 크기 옵션 추가 (XL/2XL/3XL, 작게/기본/크게)
- **코드블록** "언어 라벨 + 떠있는" 스타일 추가
- **About 페이지** 소개 내용·디자인 플레이그라운드 링크 추가
- **문서화**: decisions/0006, learnings/0007, CHANGELOG, journal

### 배경·목적

- 디자인 시스템 결정 후 구체적 스타일(폰트, 색상, 컴포넌트) 결정 도구 필요
- 한 화면에서 비교·선택 후 AI에게 반영 요청하는 워크플로우

### 다음 할 일

- 플레이그라운드에서 선택한 값을 index.css·컴포넌트에 실제 반영

---

## 2026-02-27

### 한 일

- **Hero 섹션 패딩 조정** - 브라우저 프리뷰에서 조정한 값을 소스에 반영 (`client/src/widgets/Hero/Hero.tsx`)
  - md 뷰포트: `md:py-14`(56px) → `md:py-[27px]`(27px)
- **테마 전환 시 oklch 색상 자연스럽게 이어지도록** transition 적용 (`client/src/index.css`)
  - `[data-theme] *` 및 `::before`, `::after`에 transition 추가
  - 대상 속성: background-color, color, border-color, box-shadow, fill, stroke (0.4s ease)
  - oklch 색상은 보간 시 밝기·채도·색상이 자연스럽게 변해 테마 전환이 부드럽게 보임

### 배경·목적

- Hero: 브라우저에서 직접 조정한 패딩 값을 소스에 영구 반영
- 테마 전환: blue-orange ↔ amber-cyan ↔ dark-slate 전환 시 색이 갑자기 바뀌지 않고 자연스럽게 이어지도록

### 문서화

- CHANGELOG, journal, decisions/0008(참고) 반영

---

## 2026-02-28

### 한 일

- **학습자료 페이지 - 정보처리기사 프로토타입** (Phase 1 MVP)
  - 파일 구조형 3단계 네비게이션: 학습자료 → 정보처리기사 → 과목 → 문서
  - `FileListItem` 공통 컴포넌트 (풀폭 직사각형, 세로 배치)
  - `LearningPage`, `LearningCategoryListPage`, `LearningDocListPage`, `LearningDocPage`
  - `learningInfoEngineer.ts` 인덱스 config, 샘플 md (sdlc.md, uml.md)
- **헤더 breadcrumb** - 파일 경로 형태 (`myLittleWebsite / 학습자료 / 정보처리기사 / ...`)
- **@tailwindcss/typography** - prose로 마크다운 본문 스타일링
- **FileListItem UI** - 왼쪽 패딩·높이·폰트 확대, 부가설명 제목 하단 배치

### 배경·목적

- plans/2026-02-23-learning-info-engineer.md Phase 1: 정적 md + public fetch로 MVP
- 파일 탐색기처럼 단계별 드릴다운 UX

### 문서화

- CHANGELOG, journal 반영
