class 키워드로 클래스 구현 
new 없이 클래스명만으로 생성

클래스 내부에 메소드 저으이
self - 자바에서 this
인스턴스를 통해 호출

//예시, 설명

self
참조 변수를 통해 호푸되는 메소드는 자기자신이 기본 인수로 지정된다
sefl 통해 멤버 변수, 멤버 메소드에 접근
외부에서 내부 인스턴스 변수에 접근할 떄는 참조변수가 self 역할

//예시, 설명

생성자 메소드
이미 정의된 이름 사용
def __init__(self):
인스턴스 생성 시 자동 로출
//예시, 설명

캡슐화
클래스변수
맴버변수는 모든 인스턴스에서 접근 가능
클래스 변수는 클래스 구혁에서 선언, 모든 인스턴스에서 공유
메소드 내부에서 클래스 변수에 접근하기 위해서는 클래스명 사용

private 변수
인스턴스 내부는 서로 공유 가능 외부 접근 불가능
.__ 붙여 선언
//예시, 설명

상속
class 하위 상위
//예시, 설명

super()
상위 클래스 생성자에 인수 정달이 필요ㅕ한 결우, 하위에서 상위 생성자를 별도로  호출
//예시, 설명


메소드 오버라이딩
재정의하여 기능 추가
//예시, 설명