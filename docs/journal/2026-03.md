# 2026년 3월 개발 로그

## 2026-03-24

### 한 일 (칼럼 스크랩·AI 스크랩·마크다운·문서)

- **칼럼 스크랩 DB·API**
  - `source_kind`에 `x`(X/트위터) 추가. 기존 DB: `docs/plans/2026-03-24-column-scraps-add-x-kind.sql`
  - `extra_links` JSONB: 원문 외 참고 링크 `{ label, url }[]`. 마이그레이션: `2026-03-24-column-scraps-extra-links.sql`, 신규 통합 스키마는 `2026-03-24-column-scraps-migration.sql`에 반영
  - 서버 `column-scraps` 쿼리·라우트: 생성/수정 시 `extraLinks`, 목록 검색 `q`에 추가 링크 문자열 포함
- **로컬 AI로 칼럼 폼 자동 채움**
  - `POST /api/column-scraps/ai-fill` (인증), `server/services/ollama.suggestColumnScrapFromUrl`
  - `fetch-website`: `og:image` → `coverImageUrl` 후보
  - 일반 URL: (1) 심층 분석 프롬프트 800~1800자 목표 (2) JSON으로 title·summary·bodyMd·sourceKind·tags (3) `bodyMd`가 약 550자 미만이고 분석이 길면 본문만 재생성(`expandColumnBodyMarkdown`)
  - X/Twitter: HTML fetch 안 함(오류·확장프로그램 문구가 AI에 섞이던 문제). URL·핸들만으로 전용 JSON; `bodyMd`는 `## 요약` / `## 상세 정리` 템플릿
- **클라이언트**
  - `suggestColumnScrapAiFill`, `ColumnScrapAdminDialog`에 「로컬 AI로 채우기」, 추가 링크 행 UI
  - `ColumnScrapDetailPage`: 추가 링크 섹션
- **목록 UX: ⋮ 메뉴**
  - `shared/ui/OverflowMenu.tsx`: 세로 점, 외부 클릭 닫기, `stopPropagation`
  - `ColumnPage` 카드: 클릭 시 상세 이동, 로그인 시 우측 상단 ⋮ → 편집·삭제
  - `AiDevToolsPage` 행: 본문 클릭 상세, 우측 ⋮ → `openScrapAdmin({ slug })`·삭제
  - 두 Admin 다이얼로그에서 전체 목록 리스트 제거(중복 제거)
- **레이아웃**
  - 칼럼·AI 개발도구 목록·상세: `px-5 sm:px-8 md:px-10`
- **마크다운 본문 내 YouTube**
  - `MarkdownWithMath`: `components.a` 커스터마이즈 — `href`가 유튜브면 `youtube-nocookie` iframe + 아래 원문 링크 (`parseYoutubeVideoId`)
  - 적용 범위: 칼럼/AI 스크랩 상세, 패치노트, 파일구조 브라우저 등 `MarkdownWithMath` 쓰는 모든 곳
- **문서화**
  - `docs/CHANGELOG.md`, `docs/journal/2026-03.md`(본 항목), `docs/decisions/0014-column-scraps-and-scrap-ux.md`, `docs/learnings/0020-column-scrap-markdown-youtube.md`, `docs/api-spec.md`(§6 AI 스크랩·§7 칼럼·§8 헬스·§9 참고), `docs/README.md` 인덱스

### 배경·목적

- 블로그·기사·X·유튜브 등을 한곳에 스크랩하고 메모·추가 링크로 확장
- 서버에서 읽을 수 있는 페이지는 AI로 초안을 풍부하게; X는 기술적 한계를 UX·프롬프트로 명시
- 다이얼로그는 “추가/편집 폼”에 집중, 목록 조작은 카드/행 컨텍스트에서

### 다음에 할 만한 것

- `api-spec`에 스크랩 필드 예시 JSON을 더 붙이기
- 칼럼 AI `ai-fill`에 선택적 `extraLinks` 제안(현재는 수동)

---

## 2026-03-23

### 한 일

- **즐겨찾기 링크 DB 전환 (사이트 대표 추천)**
  - 요청: localStorage → DB 저장, 모든 사용자에게 동일 목록 표시
  - 옵션 A(사이트 대표 추천) 채택: 관리자만 토글, 모두가 같은 목록 봄
  - `links` 테이블에 `is_featured`, `featured_sort_order` 컬럼 추가
  - `GET /api/links/featured` 공개 API, create/update 시 isFeatured 지원
  - FavoriteLinksWidget: fetchFeaturedLinks API 조회
  - LinksPage: 관리자(token)에게만 별 표시, 클릭 시 updateLink로 토글
  - LinkForm: "메인 추천 링크로 표시" 체크박스 (추가·수정 시)
  - 기존 `useFavoriteLinks` 훅 제거
  - 설계: `docs/plans/2026-03-23-featured-links-db-design.md`
  - decisions 0013: DB 저장 방식 ADR
- **링크 관리 페이지 · 태그 관리**
  - `/links/admin`: 「링크 관리」 / 「태그 관리」 탭, `LinksTagManager` 위젯
  - 목적·종류 태그: 상위 추가, 이름 수정, 삭제, 목적만 하위 태그 추가(`parentId`)
  - 서버: `updateClassificationValue`, `deleteClassificationValue`, `createClassificationValue`에 parentId·purpose/medium만 허용 검증
  - API: `PATCH/DELETE /api/links/values/:valueId`, `POST /values`에 `parentId` 옵션
  - 클라이언트: `updateTag`, `deleteTag`, `createTag`에 `parentId` 확장

### 배경·목적

- 데이터베이스에 저장해 영구 보존 및 공유
- 사이트 운영자가 대표 추천 링크를 지정해 방문자에게 안내
- 관리자 화면에서 태그 CRUD를 한곳에서 처리

---

## 2026-03-13

### 한 일

- **Supabase Keep-Alive + GitHub Actions**
  - Supabase 7일 비활성 정지 예고 메일 대응
  - `keepalive` 테이블(확장 버전) 생성, REST API로 ping
  - `.github/workflows/supabase-keepalive.yml` 5일마다 cron 실행
  - GitHub Secrets 설정 필요 (SUPABASE_URL, SUPABASE_ANON_KEY)
  - 학습: `docs/learnings/0019-github-actions.md` (GitHub Actions 개요)
- **링크 AI 자동 설명·분류 설계 문서화**
  - 유용한 링크 등록 시 AI가 description, 분류(valueIds) 자동 추천 기능 검토
  - 옵션: WebLLM(브라우저 내), Ollama 클라이언트(localhost), Ollama 서버
  - Ollama 클라이언트: 같은 PC에서만 동작, 다른 PC 접속 시 실패
  - Ollama 서버: 서버에 Ollama 설치 → 백엔드가 localhost:11434 호출 → 어느 PC에서 접속해도 동작
  - AWS EC2 등 클라우드 배포 시에도 동일 구조로 사용 가능
  - 설계: `docs/plans/2026-03-13-links-ai-suggest-design.md`
  - 학습: `docs/learnings/0018-local-ai-ollama-webllm.md` (Ollama, WebLLM 개념·배포 시나리오)

### 배경·목적

- 링크 등록 시 수동 입력 부담 감소
- 내 디바이스/서버에서 동작하는 AI 선호 (프라이버시·비용)
- 나중에 구현 시 참고할 수 있도록 설계·옵션 정리

### 구현 완료

- POST /api/links/ai-suggest, Ollama(lfm2:24b) 연동
- **URL만** 입력해도 AI가 제목·설명·태그 생성
- LinkForm 공통 컴포넌트 추출 (AddLinkDialog, LinksAdminPage 수정 폼)
- 진행 상태 시각화 (URL 전송 → AI 분석 → 완료)
- LinksPage 검색창 "추가", LinksAdminPage "새 링크 추가" → AddLinkDialog
- **태그 추가 기능**: POST /api/links/values, dimensionSlug(purpose|medium) 필수
- **태그 목적/종류 분류**: 모든 태그는 목적 또는 종류로만, LinkForm에 목적·종류별 새 태그 입력
- **AI 2단계 분석**: 웹사이트 fetch → AI 분석 → 메타데이터 생성
- **AI 분석 결과 보기**: Dialog로 원시 응답 표시
- **링크 카드 UI**: 제목→태그→설명 순, 태그 축소·목적/종류 구분, 설명 더보기, URL 하단
- **AI 프롬프트**: 제목=사이트명, 설명=문어체, 종류 태그 필수
- **custom→종류 마이그레이션**: `docs/plans/2026-03-13-migrate-custom-tags-to-purpose.sql`

---

## 2026-03-07

### 한 일

- **학습 기록 동적 추가** (1번 방식)
  - 런타임 폴더 스캔 API: `public/learnings/{folderName}` 스캔 → 목록 반환
  - DB 우선, 없으면 config·스캔 폴백
  - 클라이언트: API에서 섹션 목록 조회, `learning/:sectionId/*` 동적 라우팅
  - `.md` 추가 시 빌드 없이 즉시 반영
- **다중 섹션 지원 준비**
  - `server/src/config/learning-sections.ts` - 새 섹션 등록
  - `client/.../learning-parent.ts` - 브레드크럼용 스텁
  - 독후감·데이터베이스기사 폴더 구조 및 추가 방법 문서화 (decisions 0011)

### 배경·목적

- 학습 자료를 동적으로 추가하고 싶음 (빌드 없이)
- 향후 관리자 UI(3번 방식)로 확장 예정
- 정보처리기사 외 독후감·데이터베이스기사 등 추가 예정

---

## 2026-03-06

### 한 일

- **즐겨찾기 링크 위젯**
  - 설계: 방문자별 localStorage, Links 페이지 별 토글, 메인 위젯 영역
  - `useFavoriteLinks` 훅: localStorage 읽기/쓰기, storage 이벤트로 탭 간 동기화
  - LinksPage: 링크 카드에 별 아이콘, 클릭 시 즐겨찾기 토글 (preventDefault로 링크 이동 방지)
  - MainPage: FavoriteLinksWidget (BentoCard), 빈 상태 시 "즐겨찾기 링크 없습니다"
  - 설계 문서: `docs/plans/2026-03-06-favorite-links-design.md`
- **헤더 관리자 링크**
  - '관리' → '관리자 페이지', Button variant="outline" size="sm" 적용
- **디자인 룰 색상 기준** (0009)
  - 토큰별 용도: primary(메인·CTA·포커스), secondary(호버·강조·즐겨찾기), muted(비강조)
  - 호버·강조 시 secondary 사용으로 통일
- **유용한 링크 페이지 색상** - LinksPage 전체 secondary 적용
- **다크 모드 secondary** - dark-slate에서 0.35→0.6 밝기 조정 (호버 텍스트 가독성)

### 배경·목적

- 유용한 링크 중 자주 쓰는 것을 메인에서 바로 접근
- 방문자별로 저장 (localStorage), 추후 사용자 계정 연동 가능
- 색상 사용 일관성, 다크 모드 가독성

---

## 2026-03-03

### 한 일

- **유용한 링크 페이지 + 사이트 전체 관리자 인증 설계**
  - 브레인스토밍: 데이터 소스(DB), 분류(카테고리+태그 → 축+계층형 값), 관리 방식(전용 페이지), 자동 로그인(Remember Me)
  - 설계 문서: `docs/plans/2026-03-03-links-admin-design.md`
  - 구현 계획: `docs/plans/2026-03-03-links-admin-implementation-plan.md`
  - 스키마 SQL: `docs/plans/2026-03-03-links-schema.sql` (테이블 4개 + 시드)
  - 분류: 목적(디자인→이미지/3D/비디오, 개발→프론트/백엔드/LLM), 종류(웹/API/앱/CLI/확장)

### 배경·목적

- 모아둔 도구 사이트를 분류·검색해 쓸 수 있는 페이지
- 사이트 전체에서 사용할 관리자 로그인 (향후 포트폴리오·칼럼 등 확장)

### 다음 단계

- 구현 계획 실행 (Phase 1 인증 → Phase 2 DB/API → Phase 3 UI)

---

## 2026-03-03 (유용한 링크 구현·디자인)

### 한 일

- **유용한 링크 페이지 구현**
  - 인증: AuthContext, JWT 서버 검증, `/login`, `/admin`, `/links/admin`
  - 링크 API: CRUD, dimensions, 필터·검색
  - LinksPage: 검색·필터·정렬·그리드
  - LinksAdminPage: 추가/수정 폼, 카드별 수정·삭제
- **레이아웃 개선**
  - 검색창: 상단 중앙 고정
  - 좌측 사이드바: 태그 선택, 정렬, 링크 관리, 필터 초기화 (lg+)
  - 모바일: 상단 한 줄 필터
- **링크 관리 페이지 UI 정리**
  - 로그아웃 → AdminPage로 이동
  - 추가 폼 상단 항상 노출 (버튼 제거)
- **디자인 개선**
  - pill 스타일, 카드 호버 lift, 외부 링크 아이콘
  - `docs/plans/2026-02-26-design-references.md` §4 링크/북마크 레퍼런스 추가

### 문서화

- CHANGELOG, journal 반영

---

## 2026-03-01

### 한 일

- **파일 구조형 패턴 추상화·재귀 적용**
  - `FileStructureNode` 재귀 타입, `resolveFileStructurePath()` 경로 해석
  - `FileStructureBrowserPage` splat 라우트로 node-list/doc-list/doc 통합
  - `getFileStructureBreadcrumb` 재귀화
  - `learning-info-engineer` config → `file-structure-sections/` 마이그레이션
- **파일 구조형 패턴 학습 자료** 작성 (learnings 0010~0014)
  - 목차(0010-index), Part 1~5: 배경·재귀 타입·경로 해석·UI 통합·확장 방법
  - docs/README.md 학습 목록에 0010~0014 링크 추가
- **file-structure-pattern.mdc** 룰 추가 - 패턴 적용 시 참조용
- **바로가기(RightSidebar)** 플로팅 전환
  - 오른쪽 세로 중앙 고정, 메인 영역 pr-40으로 침범 방지
  - 버튼·아이콘 크기 축소

### 배경·목적

- 재귀화·추상화 패턴을 팀/미래 자신이 이해할 수 있도록 문서화
- foundation "자기서술" 원칙 충족

### 문서화

- CHANGELOG, journal 반영

---

## 2026-03-01 (Phase 4 문서화)

### 한 일

- **Phase 4 문서화 완료** (learning-folder-database-plan)
  - CHANGELOG: Phase 2·3 (DB API, 클라이언트 연동) Added 항목 반영
  - learnings 0015 보강: §8 실제 구현 경험 (env 로드 순서, Supabase 미설정 시, API 응답 형식)
  - plans 체크리스트: Phase 2·3·4 완료 표시

### 배경

- database-plan Phase 4에 남아 있던 문서화 작업 정리

---

## 2026-03-01 (학습 폴더·config·에러픽스)

### 한 일

- **학습 폴더 config 자동 생성** - `scripts/build-learning-config.mjs`, `npm run build:learning-config`
  - `public/learnings/정처기` 폴더 스캔 → `learning-info-engineer.ts` 자동 생성 (95개 md 반영)
  - 01~05 과목, 99_노트(1장~5장) 계층 구조 포함
- **에러 픽스 기록** - `docs/error-fixes/` 폴더 신설, 0001 문서 작성
  - EADDRINUSE, env 로드, API 폴백, config 자동생성, 99_노트 children 버그
- **decisions 0010** - 학습 폴더 config 자동 생성
- **docs-record** - error-fixes 섹션 규칙 추가

### 수정한 오류

1. 학습 콘텐츠 미표시 → API 빈 nodes 시 config 폴백
2. md 2개만 표시 → 폴더 스캔 스크립트로 전체 자동 생성
3. 99_노트 children 누락 → scanDir 반환값(객체) 올바르게 사용

### 문서화

- CHANGELOG, docs/README, docs-record.mdc, error-fixes/0001, decisions/0010

---

## 2026-03-01 (학습 문서 뷰어)

### 한 일

- **학습 문서 뷰어(DocViewer) 읽기용 디자인 개선**
  - `doc-reader` 클래스: 최적 읽기 폭 65ch, 줄간격 1.75
  - 본문·제목(h1~h4) 크기·여백·구분선, 인용문·코드블록·테이블 스타일
  - 문서 제목 상단 배치, "← 목록" 버튼 스타일 조정
  - 폰트: Noto Sans KR 유지 (기본), 크기 축소

### 배경·목적

- 학습자료 문서 페이지에서 읽기 편한 타이포그래피·레이아웃 적용

### 문서화

- CHANGELOG 반영
