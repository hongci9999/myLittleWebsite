연계 메커니즘 구성

@오류코드, 암호화알고리즘 채널암호화

직접 연계 - 구현단순 개발비용저렴 연계처리성능상승
인터페이스 변경에 민감, 보안이나 로직 적용 불가
간접 연계 - 다양한 환경 통합, 보안이나 로직 적용 가능, 인터페이스 변경 가능
복잡, 성능저하, 테스트 기간 소요


간접 연계 메커니즘 구성 요소

연계 데이터 생성 및 추출
프로그램에서 생성 또는 db 오브젝트를 이용
오류 발생할 경우 로드 테이블 또는 파일에 발생한 오류 내역을 상새히 기록

코드 매핑 및 데이터 변환
수신 시스템의 코드로 미핑 및 변환
송시 시스템 측의 데이터 생성 시 수행, 수신 시스템 측의 데이터 반영 전 수행
오류는 로그 테이블에 기록

인터페이스(연계) 테이블 또는 파일 생성
사전에 협의하여 정의
손의 데이블에는 송신 관련 정보 관리 위한 항목
수신에서는 수신 관리 항목 추가

로그기록
송수신 일련의 모든 활동의 결과 기록

연계 서버
송수신 관련 처리를 전송 주기마다 수행
일반적으로 수신 시스템 구간에 위치

연계데이터 반영
별도의 변환 프로그램의 구현이 필요


연계 장애 및 오류 처리 구현

장애 발생 구간
송신 시스템 - 생성,추출 코드매핑 데이터변환 인터페이스테이블등록 중 오류ㅜ 일어날 수 있음

연계 서버 - 변환 과정에서, 송수신 과정에서

수신 시스템 - 데이터 로드, 매핑 변환 , db에 데이터 반영 하는 과정에서

모든 과정에서 오류 발생 가능 오류는 모두 로그에 저장


오류 유형
시스템
프로그램
데이터'

오류 처리 절차
모니터링 - 로그확인 - 대응조치/ 재작업

오류코드 메시지 정의
e로 시작
오류 방생위치 : 연계서버 s , 연계 프로그램 a
오류 유형 : 형식 f 길이 l  코드 c 데이터 d 권한 s 필수입력 m
일련번호

오류 로그 테이블
로그 테이블 
분리하거나 통합 가능


관리 요소
오류 발생 데이터가 포함된 행row의 식별값
항목(속성,걸럼)
오규 코드와 내용
부가요소(일시,담당자 등)

채널 구간 암호화
형식, 패킷의 암호화
전체 데이터에 적용
가상 사설망 vpn 이나 유사 솔루션 적용
전용선은 비쌈

데이터 보안
암호화 처리 프로세스
암호화 한 후 연계 테이블에 등록
복호화하여 운영 db에 반영
협의하고 공유
동일한 암호화 알고리즘 라이브러리 설치

암호화 적용 대상 선정
많이 할수록 성능 저하, 적정 수준 보안


암호화 알고리즘
단방향 - 암호화만 hash기반
양방향 - 암호/복호 대칭키,비대칭키

대칭키 - 동일한 암호화 키
비대칭키 - 서로 다른 암호화 키


암호화 대상 컬럼의 데이터 길이 변경
암호화 되기 전의 값보다 커지기 떄문에 암호화 적용 대상 컬러므이 크기를 출분히 늘려준다

